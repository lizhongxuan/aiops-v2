# Multi-Agent Assembly Eval Suite

This suite is P0 smoke coverage for Code-driven Assembly trace behavior.

The cases are intentionally mock-agent friendly. They verify that the eval
schema, scorer, baseline comparison, and trace expectations are wired before
resource model or session target behavior is enabled in runtime.

No real host credentials, API keys, or production targets belong in these
fixtures.

