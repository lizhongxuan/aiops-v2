# General Ops Intent Frame Eval

This fixture set verifies that aiops-v2 classifies general operations requests through structured `IntentFrame`, `EvidenceEnvelope`, and `ToolSurfacePolicy` fields.

The cases are intentionally not tied to a single database, middleware, or vendor. They check intent, data scope, risk budget, and tool surface decisions that should apply across operations domains.
